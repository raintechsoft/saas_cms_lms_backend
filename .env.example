# Prefer 127.0.0.1 over localhost on Windows (TCP vs named pipe).
# Empty password example: mysql://root@127.0.0.1:3306/saas-cms-lms-db
DATABASE_URL="mysql://root@127.0.0.1:3306/saas-cms-lms-db"
JWT_SECRET="replace-with-at-least-32-random-characters"
JWT_EXPIRES_IN="8h"
# Keep stable across JWT rotations or existing provider credentials cannot be decrypted.
SETTINGS_ENCRYPTION_KEY="replace-with-a-separate-32-character-key"
API_PORT=4000
WEB_ORIGIN="http://localhost:5173"

# Optional — email delivery for OTP and password reset (without SMTP, codes/links log to the backend console in dev)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER="your@gmail.com"
# SMTP_PASS="your-app-password"
# SMTP_FROM="noreply@yourdomain.com"

# Optional — Google Sign-In (OAuth 2.0 Web client ID from Google Cloud Console)
# GOOGLE_CLIENT_ID="xxxx.apps.googleusercontent.com"

SUPER_ADMIN_EMAIL="admin@saas-cms-lms.local"
SUPER_ADMIN_PASSWORD="ChangeMe123!"
DEMO_ADMIN_EMAIL="admin@demo-school.local"
DEMO_ADMIN_PASSWORD="ChangeMe123!"
